using UnityEngine;

public class MONOSCHURL : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("MONOSCHURL is active, running Update method, frame by frame, as expected, in Unity, the game engine, during gameplay, rendering frames continuously, ensuring smooth animations and interactions, as designed.");
    }
}
